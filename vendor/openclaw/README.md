# OpenClaw Vendor Directory

Install OpenClaw package(s) here. This folder is treated as read-only by project rules.

## Phase 1 setup
- Run from project root: `npm run setup:openclaw`
- OpenClaw runtime installs under this vendor folder and is consumed via gateway/client flow.
